---
summary: "Dockerfile build stage switched to node:24-slim for tsx compatibility."
breaking: false
security: false
---

# 0.1.3 — 2026-05-25

## Fixed

- **Dockerfile build stage** switched to `node:24-slim` so `tsx` resolves correctly when running `npm run build` inside the container. `oven/bun:1.3` doesn't include Node.js utilities needed by tsx during the build phase.
